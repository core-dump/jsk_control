- ページ切り替え後にmidiを動かさずに元のページに切り替えるとジャンプする。
    - これが解消できればついでにswitch_pageの場合分けが減らせる。
    - 解決したけど場合分けを減らすと現在ページに飛んだときに折れ点が変わるけどどうしようか？
- Arrayじゃない方は単独で使えるようにしたい。
    - prevのような複数ページ用の処理はArrayにのみ実装した方がきれい？
    - 1ページならトピック名を簡単にしたい。
- コメント書くか
- 一度出さないと各ページのトピック名が見えない
- よく考えると、折れる点がoriginである必要はない。折れる点を指定すると2直線が決定され、今はoriginを折れる点にしている、折れる点を0.5に固定して直線を計算する方法もある。
    - だめ。(in, out)=(0.3, 0.9)を実現するにはin=0.5でout>1になる。
- midi-abs -> angle-vectorではなくmidi-abs -> midi-relative -> angle-vector とすると折れ点が2つ出てきてこれまでの実装と異なるけどどうしよう。
    - max, minを持っておいて、midi_relativeが直接間接角を計算するというのは？
    - 実は今までの実装も折れ点が2個ある気がしてきた。midi-ref-list(= input-origin), midi-origin-list(= output-origin), ref-angle-alist(まだない)の3変数。
